// we use the variables to store information and use it in our code

// we create them with the word "Var"
// we give it a name
// and we give it a value

// we print it to the console.

// we select it and do left click or double click, and select Code Runner

// the first one is -> var <-
// the semicolon is for good practice
var Name = "Diego";

console.log(Name);

var age;

console.log(age);

age = 30;

console.log(age);


var person = {
  name: "Diego",
  age: '30'
};

console.log(person);